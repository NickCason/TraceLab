# Test Organization

- `test/*.test.js` contains focused unit tests for single utilities.
- `test/integration/*.integration.test.js` contains scenario-based workflow and persistence regression tests using realistic fixtures.
- `test/fixtures/**` contains reusable CSV/project fixture files shared by integration tests.
